# TEST EXECUTION GLOBAL CONFIGURATIONS

# Tier may be prod or production / cit / uat / prod
env=cit

# Browser
browser=chrome

# Selenium Server: this could be local or depending on
seleniumserver=local

# Selenium Hub URL: if Selenium Server is remote, hub URL point to complete hub path
#seleniumserverhost=http://

# Timeout is used to define implicit wait time i.e. global wait time in seconds for page elements
timeout=60

driverpath = src/test/resources/drivers/chromedriver

# take screenshot, put value as yes or no (default) and provide a path to save screenshots
take-screenshot=yes
screenshot-path=./target/screenshots/

# API host names
authentication_host=https://hronesanity-authentication.azurewebsites.net
api_host=https://hronesanity-webapi.azurewebsites.net/api

#API Authentications
username=tech/tech00/01
password=QHnu0LQY8Dutm5FMM8fBgg==
grant_type=password
companyDomainCode=sanity